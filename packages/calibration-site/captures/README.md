# Calibration Captures

Each subdirectory is one immutable native-observation bundle. See
`../capture/README.md` for the workflow.

Never hand-edit `native.web.txt`. Do not replace `origin.html` when recomparing after parser
changes. Create a new capture ID for a new deployment or a fresh native observation.
